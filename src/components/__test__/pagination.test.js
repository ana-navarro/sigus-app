import { usePagination } from "../../hooks/usePagination";
import { Pagination } from "../Pagination";

// jest.mock(
//     usePagination = ({
//         currentPage: 1,
//         totalCount: 10,
//         siblingCount: 2,
//         pageSize: 10,
//     })
// );

// const sut = render(<Pagination />, mock);

describe('Pagination', () => {
    test('should render first page', () => {
        expect().toBeTruthy()
    })
})

